#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
CrewAI A-Stock - 智能推荐Crew

6个Agent协作完成股票推荐：
1. 复盘分析师 → 2. 市场情报官 → 3. 智能选股师 → 4. 多维分析师(并行) → 5. 风险管理官 → 6. 投资决策官

✅ 支持多用户隔离：每个session_id有独立的Crew实例
"""

from crewai import Crew, Task, Process
from src.agents.smart_agents import create_all_smart_agents
from datetime import datetime
from loguru import logger


def create_smart_recommendation_crew(session_id: str = 'default'):
    """
    创建智能推荐Crew

    Args:
        session_id: 用户session_id，用于数据隔离

    Returns:
        Crew实例
    """
    logger.info(f"🏗️ 创建智能推荐Crew: session_id={session_id[:8]}...")

    # ✅ 设置当前session_id（使用contextvars）
    from src.agents.tools.database_tools import set_current_session_id
    set_current_session_id(session_id)

    # 创建所有Agent
    agents = create_all_smart_agents()
    
    # ========================================
    # Task 1: 复盘昨日表现 + 今日表现 + 深度经验总结
    # ========================================
    task_review = Task(
        description='''复盘昨日推荐的股票表现，分析今日推荐的当前表现，分析所有历史策略的胜率，深度总结经验教训，为市场情报官提供决策依据。

## 📋 你需要完成的任务（8步法）

### 第1步：分析最近一个交易日推荐胜率（✅ 自动跳过周末）
**工具**：analyze_recommendation_performance(days=-1)

**任务**：
- 查询最近一个交易日推荐的所有股票（自动跳过周末/节假日）
- 统计推荐后股票是否上涨
- 计算推荐胜率和平均收益
- 按策略分组统计推荐表现

**重要说明**：
- ✅ days=-1 会自动查找最近一个有推荐记录的交易日
- ✅ 如果今天是周一，会自动查找上周五的推荐
- ✅ 如果最近7天都没有推荐，会返回"最近7天没有推荐股票"

---

### 第1.5步：分析今日推荐当前表现（🔴 新增）
**工具**：analyze_recommendation_performance(days=0)

**任务**：
- 查询今日推荐的所有股票
- 统计当前价格相对推荐价的涨跌
- 计算今日推荐的当前胜率和平均收益
- 按策略分组统计今日表现

**重要说明**：
- ✅ 今日推荐使用实时价格，不是T+1收盘价
- ✅ 如果今日没有推荐，跳过此步骤
- ✅ 今日表现仅供参考，不作为策略评估依据

---

### 第2步：分析策略表现
**工具**：query_strategy_performance

**任务**：
- 查询所有策略最近7天的胜率和收益率（策略数量是动态的）
- 找出表现最好和最差的策略
- **识别策略的适用场景**（什么市场环境下策略有效？）

**策略评估方法**：
你需要根据胜率、交易次数、收益率综合判断策略优劣：
- 胜率越高越好，交易次数越多越可信
- 综合胜率和收益率，给出策略排名和评价

---

### 第3步：成功案例提炼（最关键！）
**目标**：从成功案例中提炼可复制的成功模式

**任务**：
1. **识别成功案例**（你自己判断什么是"成功"）：
   - 推荐后涨幅明显的股票
   - 买入后盈利较好的股票
   - 表现优秀的策略

2. **提炼成功共性**（5W1H分析法）：
   - **What（什么）**：成功的股票有什么共同特征？
   - **When（何时）**：成功发生在什么市场环境下？
   - **Where（何处）**：成功发生在哪些板块/行业？
   - **Who（谁）**：哪些策略成功率高？
   - **Why（为什么）**：成功的根本原因是什么？
   - **How（如何）**：如何复制成功？

3. **输出成功模式**（至少1个）：
   ```
   成功模式1：牛市+HOT市场+热点板块+龙头战法
   - 市场环境：牛市，市场情绪火热，热点板块活跃
   - 策略逻辑：追涨热点板块龙头股，快速轮动
   - 筛选参数：你需要根据市场情况自己设计具体参数
   - 成功案例：XXX（涨幅高）、YYY（涨幅高）
   - 成功原因：市场环境匹配，选股逻辑正确，买入时机好
   - 复制方法：在牛市+HOT市场+有热点时，继续使用龙头战法
   ```

---

### 第4步：失败案例分析（最关键！）
**目标**：从失败案例中找出问题根源，避免重复犯错

**任务**：
1. **识别失败案例**（你自己判断什么是"失败"）：
   - 推荐后跌幅明显的股票
   - 买入后亏损较大的股票
   - 表现不佳的策略

2. **诊断失败原因**（鱼骨图分析法）：
   - **原因1：推荐质量问题**（选股逻辑错误、技术面误判等）
   - **原因2：市场环境不匹配**（策略不适合当前市场）
   - **原因3：执行问题**（买入时机不好、买入价格过高）
   - **原因4：风险控制问题**（止损不及时、风险评估不足）
   - **原因5：外部因素**（系统性风险、突发事件）

3. **输出失败教训**（至少1个）：分析失败原因和改进方法

---

### 第5步：策略适用场景识别
**目标**：明确每个策略的最佳适用场景和失效场景

**任务**：
1. **统计策略在不同场景下的胜率**：
   - 牛市胜率 vs 熊市胜率 vs 震荡市胜率
   - HOT市场胜率 vs WARM市场胜率 vs NEUTRAL市场胜率 vs COLD市场胜率

2. **识别最佳适用场景**（你需要根据胜率数据自己判断）：
   - 胜率高的场景 → 最佳适用场景
   - 胜率中等的场景 → 适用场景
   - 胜率低的场景 → 不适用场景

3. **输出场景识别结果**（至少Top 3策略）：
   ```
   策略：龙头战法
   - 最佳适用场景：牛市+HOT市场+有明确热点（胜率高）
   - 适用场景：震荡市+WARM市场+有热点（胜率中等）
   - 不适用场景：熊市+COLD市场+无热点（胜率低）
   - 建议：在牛市和HOT市场时优先使用，熊市和COLD市场时避免使用
   ```

---

## 📊 输出格式（必须包含以下所有部分）

```
=== 第1部分：最近一个交易日推荐表现 ===
推荐数量：X只
盈利数量：X只
亏损数量：X只
推荐胜率：X%
平均收益率：X%

详细表现：
1. 股票A（策略：集中持仓优质股）：盈利X%，推荐价X元，次日收盘X元
2. 股票B（策略：龙头战法）：亏损X%，推荐价X元，次日收盘X元
...

=== 第1.5部分：今日推荐当前表现 ===
推荐数量：X只
当前盈利数量：X只
当前亏损数量：X只
当前胜率：X%
当前平均收益率：X%

详细表现：
1. 股票A（策略：XXX）：当前盈利X%，推荐价X元，当前价X元
2. 股票B（策略：XXX）：当前亏损X%，推荐价X元，当前价X元
...

**重要说明**：
- ✅ 今日表现仅供参考，不作为策略评估依据
- ✅ 如果今日没有推荐，显示"今日暂无推荐"

=== 第2部分：策略排名（最近7天，按胜率排序）===
【共X个策略，展示Top 10】

1. 集中持仓优质股：
   - 胜率：X%
   - 平均收益率：X%
   - 交易次数：X次
   - 表现：优秀/良好/一般/较差

2. 龙头战法：
   - 胜率：X%
   - 平均收益率：X%
   - 交易次数：X次
   - 表现：优秀/良好/一般/较差

... (最多展示Top 10策略)

=== 第3部分：成功案例提炼 ===
【成功模式1】
- 市场环境：[详细描述]
- 策略逻辑：[详细描述]
- 筛选参数：[详细描述]
- 成功案例：[具体股票和涨幅]
- 成功原因：[深度分析]
- 复制方法：[可执行的方法]

【成功模式2】（如果有）
...

=== 第4部分：失败案例分析 ===
【失败教训1】
- 市场环境：[详细描述]
- 策略逻辑：[详细描述]
- 失败案例：[具体股票和跌幅]
- 失败原因：[深度分析，使用鱼骨图分析法]
- 改进方法：[可执行的改进方法]

【失败教训2】（如果有）
...

=== 第5部分：策略适用场景识别 ===
【策略1：龙头战法】
- 最佳适用场景：[详细描述+胜率]
- 适用场景：[详细描述+胜率]
- 不适用场景：[详细描述+胜率]
- 建议：[具体建议]

【策略2：低吸埋伏】
- 最佳适用场景：[详细描述+胜率]
- 适用场景：[详细描述+胜率]
- 不适用场景：[详细描述+胜率]
- 建议：[具体建议]

... (至少Top 3策略)

=== 第6部分：给市场情报官的建议 ===
【推荐策略】（基于历史胜率和适用场景）
- 策略1：[策略名称]，胜率X%，适用场景：[详细描述]
- 策略2：[策略名称]，胜率X%，适用场景：[详细描述]

【不推荐策略】（基于历史胜率和适用场景）
- 策略3：[策略名称]，胜率X%，不适用场景：[详细描述]
- 策略4：[策略名称]，胜率X%，不适用场景：[详细描述]

【关键提示】
- 当前市场环境：[牛市/熊市/震荡市]
- 当前市场情绪：[HOT/WARM/NEUTRAL/COLD]
- 建议策略方向：[激进/稳健/保守]
```

---

## ⚠️ 重要提示

- ✅ 策略数量是动态的，不限于8种
- ✅ 分析所有历史使用过的策略
- ✅ 按胜率排序，展示Top 10策略
- ✅ **必须提供成功案例提炼和失败案例分析**
- ✅ **必须识别策略的适用场景**
- ✅ 所有结论必须基于数据，不能主观臆断
- ✅ 这些数据将直接影响市场情报官的决策
''',
        expected_output='昨日表现总结 + 策略排名 + 成功案例提炼 + 失败案例分析 + 策略适用场景识别 + 推荐策略',
        agent=agents['performance_analyst']
    )
    
    # ========================================
    # Task 2: 分析市场环境
    # ========================================
    task_market = Task(
        description='''分析市场情绪和新闻热点，结合复盘结果，动态生成筛选参数和推荐策略。

你需要按照以下顺序执行：

**第1步：深度分析复盘分析师的策略排名**（🔴 最重要！必须第一步执行）
   - **这是最重要的决策依据，必须优先分析**
   - 查看复盘分析师提供的策略胜率排名
   - 识别表现优秀和不佳的策略
   - 分析成功案例和失败案例
   - 理解策略的适用场景
   - **如果历史交易记录较少，进入冷启动模式（后续步骤会说明）**

**第2步：计算推荐数量**（使用"根据总资产计算推荐数量"工具）
   - 推荐数量由工具根据总资产自动计算
   - **即使满仓，也要推荐股票**（投资决策官会排除已持仓股票，推荐新股）

**第3步：分析市场环境**（可选，如果有工具则执行）
   - **3.1 分析今日涨停板**（如果有"分析今日涨停板"工具）
     * 使用"分析今日涨停板"工具
     * **📋 在你的分析输出中完整展示涨停板分析结果**
     * 提取今日涨停股票的热点题材（哪些板块涨停多）
     * 识别强封板股票（封单金额大 = 主力资金强）
     * 根据涨停家数判断市场情绪（涨停多 = 情绪火热）
     * **如果API返回错误（如周末无数据），记录错误信息并继续**
   - **3.2 获取市场情绪数据**（使用"获取市场情绪数据"工具）
     * 涨停家数、跌停家数
     * 市场状态（HOT/WARM/NEUTRAL/COLD）
   - **3.3 搜索最新市场新闻**（使用"搜索市场新闻热点"工具）
     * 识别当前热点题材
     * 分析政策导向
   - **3.4 分析热点题材趋势**（可选，如果发现明确热点题材）
     * 如果从新闻中识别出明确的热点题材
     * 使用"分析话题热度趋势"工具，分析该题材的热度变化
     * 判断热点是否持续上升、稳定还是下降

**第4步：综合判断最优策略**（不要使用固定规则，要根据数据决策）
   - 综合复盘分析师的策略排名、市场情绪、新闻热点
   - **优先考虑历史胜率高的策略**
   - 根据当前市场环境调整策略

**第5步：动态生成具体的筛选参数**（必须是具体数值）
   - 根据选定的策略，生成筛选参数
   - 参数必须是具体数值（你需要根据策略逻辑自己设计）
   - **✅ 重要：如果策略涉及特定板块或概念，必须填写sectors或concepts参数**
     * 例如："降息受益龙头" → sectors="银行,地产,基建"
     * 例如："AI龙头追击" → concepts="AI,算力,芯片"
     * 例如："新能源轮动" → concepts="新能源,锂电池,光伏"
     * 例如："科技成长精选" → concepts="半导体,芯片,5G"

【决策逻辑】

**🔴 冷启动模式判断**
- 首先检查复盘分析师报告中的历史交易记录数
- 如果历史交易记录较少 → 进入冷启动模式
- 如果历史交易记录足够 → 进入正常模式

**🔴 冷启动模式**（历史交易记录较少）：

在冷启动模式下，你需要**自主设计策略**，而不是从固定列表中选择。

**策略设计思维框架**（5步法）：

**第1步：分析市场阶段**（使用identify_market_phase工具）
- 牛市/熊市/震荡市？
- 趋势强度如何？
- 市场处于什么位置（顶部/底部/中部）？

**第2步：分析市场情绪**（使用get_market_sentiment工具）
- 涨停家数：反映市场活跃度
- 市场状态：HOT/WARM/NEUTRAL/COLD
- 资金流向：流入还是流出？
- 情绪指标：恐慌还是贪婪？

**第3步：分析新闻热点和涨停板**
- **3.1 涨停板分析**（如果有"分析今日涨停板"工具）
  * 使用"分析今日涨停板"工具
  * **📋 在你的分析输出中完整展示涨停板分析结果**（包括热点题材、强封板股票、策略建议）
  * 提取今日涨停股票的共同题材（哪些板块涨停多）
  * 识别强封板股票（封单较大 = 主力资金强）
  * 判断市场情绪（涨停家数多 = 情绪火热）
  * **如果没有此工具，跳过此步骤**
- **3.2 新闻热点分析**（使用search_market_news工具）
  * 当前有哪些热点题材？
  * 哪些板块受到政策支持？
  * 有没有突发事件或重大新闻？
  * 市场关注度最高的是什么？

**第4步：分析资产规模**
- 总资产规模决定了风险承受能力
- 小资金：可以更激进，集中持仓
- 中资金：适度分散，平衡风险
- 大资金：充分分散，稳健为主

**第5步：综合设计策略**
- 根据市场阶段、市场情绪、新闻热点、资产规模，**创造性地设计策略**
- 策略名称要**简洁、描述性强**（2-8个字）
- 策略逻辑要**清晰、可执行**
- 筛选参数要**符合策略逻辑**

**策略创新示例**（仅供参考，不要照搬，你需要根据市场情况自己设计参数）：

1. **牛市 + HOT市场 + 热点板块 + 小资金**（追涨策略）
   - 策略名称："XX龙头追击"
   - 策略逻辑：集中持仓热点板块龙头，追强势股
   - 筛选参数：你需要根据市场情况自己设计涨幅、换手率、成交额等参数
   - ⚠️ 风险：容易买在高点，T+1日可能冲高回落

2. **震荡市 + WARM市场 + 热点板块 + 小资金**（低吸策略）
   - 策略名称："XX低吸埋伏"
   - 策略逻辑：在热点板块回调时买入，等待反弹
   - 筛选参数：你需要根据市场情况自己设计涨幅、换手率、成交额等参数
   - ✅ 优势：成本更低，T+1日更容易反弹

3. **牛市 + WARM市场 + 科技热点 + 中资金**（板块策略）
   - 策略名称："科技龙头精选"
   - 策略逻辑：布局科技板块龙头，中线持有
   - 筛选参数：你需要根据市场情况自己设计涨幅、换手率、成交额等参数
   - ✅ 优势：板块轮动，持续性强

4. **震荡市 + WARM市场 + 新能源热点 + 大资金**（板块轮动）
   - 策略名称："新能源轮动"
   - 策略逻辑：跟随新能源板块轮动，分散持仓
   - 筛选参数：你需要根据市场情况自己设计涨幅、换手率、成交额等参数

5. **震荡市 + HOT市场 + 题材轮动快 + 小资金**（热点策略）
   - 策略名称："热点快打"
   - 策略逻辑：快速切入当日最热题材，短线操作
   - 筛选参数：你需要根据市场情况自己设计涨幅、换手率、成交额等参数

**重要原则**：
- ❌ 不要使用固定的"市场状态→策略名称"映射
- ✅ 要根据实时市场信息**创造性地设计策略**
- ✅ 策略名称可以参考历史，也可以完全创新
- ✅ 策略逻辑要符合当前市场环境
- ✅ 说明原因："当前处于冷启动阶段，暂无足够历史数据，根据市场阶段、市场情绪、新闻热点综合设计策略"

**✅ 正常模式**（历史交易记录足够）：

**第1步：分析策略胜率**（最重要！）
- 查看复盘分析师提供的策略最近一段时间的胜率和收益率
- 找出胜率高的策略（表现优秀）
- 找出胜率低的策略（表现不佳）
- **优先选择胜率高的策略，坚决避免胜率低的策略**

**第2步：分析市场情绪**
- 涨停家数：反映市场活跃度
- 市场状态：HOT/WARM/NEUTRAL/COLD
- 考虑市场情绪对策略的影响

**第3步：分析新闻热点**
- 识别当前热点题材
- 考虑热点对策略的影响

**第4步：综合决策**
- **🔴 必须优先遵循市场阶段分析的策略建议**：
  * 市场阶段分析工具（identify_market_phase）会给出明确的策略建议
  * 例如："稳健策略，低吸埋伏、高低切换"
  * **你必须优先采纳这些建议**，而不是自己随意创造策略
  * 如果市场阶段建议"低吸埋伏"，就不要选择"追涨龙头"
  * 如果市场阶段建议"稳健策略"，就不要选择"激进追高"
- **⚠️ 禁止使用过时策略名称**：
  * ❌ 禁止使用"降息"、"降准"相关策略（降息是6个月前的旧闻，已被市场消化）
  * ❌ 禁止使用"疫情"、"复工复产"相关策略（已过时）
  * ❌ 禁止使用无意义的策略名称（如"满仓单调策略"、"优质分散策略"）
  * ✅ 只使用**有明确选股逻辑**的策略名称（如"龙头战法"、"低吸埋伏"、"题材轮动"）
- **策略选择原则**：
  * **优先遵循市场阶段分析的策略建议**
  * 综合策略胜率、市场情绪、涨停板分析
  * 根据当前市场情况，自主设计最优策略
  * 策略名称应该**简洁、描述性强**（如"龙头战法"、"低吸埋伏"、"题材轮动"等）
- **可以参考历史策略，也可以创新**
- **不要使用固定规则**（除非在冷启动模式）
- **要根据历史数据和当前市场灵活决策**

【输出格式】（必须包含具体数值）

**🔴 第一步：使用"保存Agent上下文"工具保存关键数据**

你必须调用3次"保存Agent上下文"工具，保存以下数据：

1. 保存推荐数量：
```python
save_agent_context(
    context_type='recommended_count',
    context_data={'count': X, 'reason': '总资产XX万，推荐X只'}
)
```

2. 保存策略名称：
```python
save_agent_context(
    context_type='strategy_name',
    context_data={'name': '策略名称', 'reason': '市场XX，历史胜率XX%'}
)
```

3. 保存筛选参数（你需要根据策略逻辑自己设计参数）：
```python
save_agent_context(
    context_type='screening_params',
    context_data={
        'price_change_min': X,  # 涨幅下限（%）
        'price_change_max': X,  # 涨幅上限（%）
        'turnover_min': X,      # 换手率下限（%）
        'volume_min': X,        # 成交额下限（亿元）
        'sort_by': 'price_change',
        'max_results': X,       # 返回数量
        # ✅ 板块筛选（根据策略名称填写）
        'sectors': '',      # 板块筛选（逗号分隔）："银行,地产,基建"
        'concepts': ''      # 概念筛选（逗号分隔）："AI,算力,芯片"
    }
)
```

**🔴 第二步：输出分析报告**

```
=== 资产分析 ===
[直接引用"根据总资产计算推荐数量"工具的输出结果]
当前总资产：X元
持仓数量：X只
可用资金：X元
推荐数量：X只（只跟总资产有关，与持仓无关）
推荐理由：[工具给出的理由]

⚠️ 重要：即使满仓，也要推荐股票！投资决策官会排除已持仓股票，推荐新股。

=== 市场分析 ===
市场状态：HOT/WARM/NEUTRAL/COLD
涨停家数：X只
热点题材：[热点1, 热点2, ...]

=== 策略分析 ===
历史策略胜率排名：
1. 策略1：胜率X%，收益率X%
2. 策略2：胜率X%，收益率X%
...

=== 决策结果 ===
推荐策略：[根据市场情况自主设计，例如：龙头战法、集中持仓优质股、激进成长策略等]
选择理由：[详细说明为什么选择这个策略]

=== 筛选参数 ===（你需要根据策略逻辑自己设计参数）
price_change_min = X      # 涨幅下限（%）
price_change_max = X      # 涨幅上限（%），**⚠️ 严格限制：最高不超过9%，避免涨停股（9.5%）**
turnover_min = X          # 换手率下限（%）
turnover_max = 100        # 换手率上限（%）
volume_min = X            # 成交额下限（亿元）
amplitude_min = X         # 振幅下限（%）
sort_by = "price_change"  # 排序字段（turnover/price_change/volume）
max_results = X           # 返回数量
sectors = "X,X,X"         # 板块筛选（逗号分隔），例如："银行,地产,基建"
concepts = "X,X,X"        # 概念筛选（逗号分隔），例如："AI,算力,芯片"
```

**🔴 重要提示**：
- ✅ 必须使用"根据总资产计算推荐数量"工具
- ✅ 严格按照工具返回的数量推荐股票（不要多也不要少）
- ✅ 推荐数量只跟总资产有关（你需要根据总资产自己判断推荐数量）
- ✅ **即使满仓，也要推荐股票**（投资决策官会排除已持仓股票，推荐新股）

**策略设计建议**：
- ✅ 策略名称要简洁（2-8个字）
- ✅ 策略名称要描述性强（让人一看就懂）
- ✅ 可以参考历史策略，也可以创新
- ✅ 策略会自动记录到数据库，用于后续胜率统计

【参数设置建议】

**🚨 涨停股过滤规则（所有策略必须遵守）**：
- **price_change_max 必须 ≤ 9%**（涨停股是9.5%，必须排除）
- **禁止设置 price_change_max ≥ 9.5%**（会包含涨停股，买不进去）

**参数设计原则**（你需要根据策略逻辑自己设计参数）：
- **追涨策略**：涨幅较高，换手率较高，成交额较大
- **低吸策略**：涨幅较低或回调，换手率适中，成交额适中
- **轮动策略**：涨幅适中，换手率适中，成交额适中

**新闻过滤说明**：
- **新闻过滤**（enable_news_filter）：True/False
  * True：只保留有最近新闻的股票（提高热度和关注度）
  * False：不过滤
- **新闻时效性**（news_hours）：整数，你需要根据策略自己判断时效性

**新闻过滤使用建议**：
- **题材轮动策略**：建议启用新闻过滤
  * 理由：题材轮动需要热点支撑，有新闻的股票更容易成为热点
- **龙头战法策略**：建议启用新闻过滤
  * 理由：龙头股通常有新闻催化
- **低吸埋伏策略**：不建议启用新闻过滤
  * 理由：低吸埋伏是提前布局，可能还没有新闻

**重要原则**：
- ❌ 不要死板套用上述建议
- ✅ 要根据市场情况灵活调整
- ✅ **目标是筛选出足够数量的候选股**（给AI更大选择空间）
- ✅ **如果候选股太少，说明条件太严格，需要放宽**
- ✅ 优先考虑策略胜率，而不是市场状态
- ⚠️ **板块筛选会显著减少候选股数量，谨慎使用**（只在有明确热点时启用）
- ⚠️ **新闻过滤会显著减少候选股数量，谨慎使用**（只在新闻驱动策略时启用）
- ✅ **默认情况下，不启用板块筛选、新闻过滤**（保证候选股数量充足）

注意：
- 必须输出具体的数值参数
- 智能选股师将直接使用这些参数
- **策略选择必须基于历史胜率，不要使用固定规则**
''',
        expected_output='市场状态 + 热点题材 + 推荐策略 + 具体筛选参数',
        agent=agents['market_intelligence'],
        context=[task_review]
    )
    
    # ========================================
    # Task 3: 智能筛选候选股
    # ========================================
    task_screen = Task(
        description='''基于市场情报官保存的筛选参数，从全市场筛选符合策略的候选股。

**🔴🔴🔴 混合筛选策略 + 降级规则（必须严格遵循）🔴🔴🔴**

**第一阶段：板块筛选**（如果有sectors/concepts参数）
- 使用原始参数筛选，目标：足够数量的候选股
- 如果板块筛选结果太少，进入降级流程

**第二阶段：全市场补充**（如果板块筛选不够）
- 清空sectors/concepts，从全市场筛选
- 与板块筛选结果合并去重
- 目标：合计足够数量的候选股

**降级优先级**（每次只调整一个参数）：
| 降级次序 | 调整参数 | 调整方法 | 说明 |
|---------|---------|---------|------|
| 第1次 | min_market_cap | 减半 | 放宽市值下限（风险最小）|
| 第2次 | max_market_cap | 增加 | 放宽市值上限 |
| 第3次 | price_change_min | 减少 | 放宽涨幅下限 |
| 第4次 | turnover_min | 减半 | 放宽换手率下限 |
| 第5次 | volume_min | 减半 | 放宽成交额下限 |
| 第6次 | sectors/concepts | 清空（全市场补充）| 最后手段 |

**完整示例**：
```
原参数：price_change_min=X, turnover_min=X, volume_min=X,
       min_market_cap=X亿, max_market_cap=X亿, concepts="XX"

=== 第一阶段：板块筛选 ===
第1次筛选（板块）：concepts="XX"，结果：X只 ❌

第1次降级（放宽市值下限）：min_market_cap减半
第2次筛选：X只 ❌

第2次降级（放宽市值上限）：max_market_cap增加
第3次筛选：X只 ❌

第3次降级（放宽涨幅）：price_change_min减少
第4次筛选：X只 ✅ 板块筛选完成

=== 第二阶段：全市场补充 ===
当前：X只，目标：足够数量，需补充：X只
全市场筛选（concepts=""）：补充X只
合并去重后：X只 ✅
```

**🚨 严禁**：
- ❌ 一次调整多个参数
- ❌ 筛选失败后直接放弃
- ❌ 跳过降级步骤
- ❌ 不做全市场补充就结束

---

**执行步骤**：

1. **读取筛选参数**
   ```python
   params = load_agent_context('screening_params')
   ```

2. **板块筛选**（带sectors/concepts）
   - 调用dynamic_screen_stocks，使用完整参数
   - 如果结果太少，按降级顺序调整参数重试

3. **全市场补充**（如果板块筛选不够）
   - 清空sectors/concepts，保留其他参数
   - 再次调用dynamic_screen_stocks
   - 合并两次结果，去重

4. **输出结果**

---

**输出格式**：
```
=== 板块筛选 ===
参数：concepts="XX", min_market_cap=X亿, price_change_min=X
结果：X只

=== 全市场补充 ===
参数：concepts="", 其他不变
补充：X只

=== 最终候选股（X只）===
1. 股票名称(代码) - 涨幅X% - 换手率X% - 成交额X亿 - 来源：板块/全市场
2. ...

✅ 已保存候选股列表到AgentContext
```

注意：
- **必须使用dynamic_screen_stocks工具**
- **必须传入市场情报官提供的具体参数值**
- **必须使用save_agent_context保存候选股列表**
- 不要使用策略名称，使用具体数值
- **返回足够数量的候选股，给AI更大选择空间**
''',
        expected_output='候选股票列表 + 保存成功确认',
        agent=agents['smart_screener'],
        context=[task_market]
    )
    
    # ========================================
    # Task 4: 多维分析（并行执行）
    # ========================================
    task_analysis = Task(
        description='''对候选股进行5维并行分析：技术+资金+基本面+新闻+社区情绪，给出综合评分。

**🔴🔴🔴 强制要求：必须先读取候选股列表！🔴🔴🔴**

**第1步（强制）：读取候选股列表**
- **必须调用工具**：load_agent_context('candidate_stocks')
- **不要自己选择股票**：必须分析所有候选股
- **不要只分析少量股票**：必须分析所有候选股

**第2步（强制）：解析JSON，提取所有候选股代码**
- 从JSON中提取所有股票代码
- 格式化为逗号分隔的字符串：'600000,000001,600519,...'

**第3步（强制）：🚀 使用自动分批并行分析工具（一次性分析所有股票）**

**🔴🔴🔴 重要：使用新工具 `analyze_all_stocks_auto_batch` 🔴🔴🔴**

**新工具优势**：
- **一次调用**：无需手动分批，工具内部自动分批
- **多批次并行**：所有批次同时执行，大幅提升速度
- **性能提升**：35只股票，传统方式需要7次串行调用，新方式只需1次调用
- **耗时对比**：
  - 传统方式：7批 × 30秒/批 = 210秒（3.5分钟）
  - 新方式：1次调用 ≈ 30-40秒（接近单批耗时）

**使用方法**（假设有35只候选股）：

```python
analyze_all_stocks_auto_batch(
    stock_codes="600000,000001,600519,000796,600301,600036,601166,601857,601988,601398,600016,600030,600048,600050,600104,600111,600150,600276,600309,600340,600362,600406,600438,600519,600547,600585,600588,600690,600703,600745,600809,600837,600887,600900,600919",
    batch_size=5  # 每批5只股票（默认值，可省略）
)
```

**工具会自动完成以下工作**：
1. 自动分批（每批5只）：35只股票 → 7批
2. 7批并行执行（而不是串行等待）
3. 每批内部的5只股票也并行分析
4. **详细分析自动保存到 AgentContext**（避免上下文超长）
5. 只返回简洁的评分表格

**重要提示**：
- ✅ 只需调用1次工具，无需手动分批
- ✅ 工具内部自动并行处理所有批次
- ✅ 大幅提升速度（接近单批耗时）
- ✅ 详细分析已保存到 AgentContext，不会上下文超长
- ✅ 下一步需要调用 `load_agent_context('stock_analysis_details')` 读取详细分析

---

**第4步（强制）：从 AgentContext 读取详细分析，进行评分**

工具返回的只是统计信息，详细分析已保存到 AgentContext。

**必须执行**：
```python
load_agent_context('stock_analysis_details')
```

这会返回所有股票的详细分析（JSON格式），包括：
- 技术指标完整分析
- 资金流向完整分析
- 基本面完整分析
- 新闻完整分析
- 逐笔交易完整分析
- 社区情绪完整分析

**评分要求**：
- 根据详细分析，给出每只股票的6维评分（0-100分）
- 计算默认权重评分（均等权重）
- 计算动态权重评分（根据市场环境调整）
- 识别异常信号和风险等级

---

**🔴 重要：6维分析说明**
- **维度1：技术面**（MACD、KDJ等技术指标）
- **维度2：资金流向**（近5日资金流向趋势、量价配合）
- **维度3：基本面**（市值、PE、ROE等财务指标）
- **维度4：新闻面**（新闻情绪、政策利好）
- **维度5：逐笔交易**（当日实时逐笔数据，主力行为分析）⭐ 核心数据源
- **维度6：社区情绪**（雪球、东财股吧、淘股吧）

**重要提示**：
- 使用 `analyze_all_stocks_auto_batch` 工具，一次性分析所有股票
- 工具内部自动分批并行执行，大幅提升速度
- 详细分析自动保存到 AgentContext，避免上下文超长
- 然后从 AgentContext 读取详细分析，进行评分

---

**第5步（强制）：根据详细分析，对每只股票进行6维评分**

从 AgentContext 读取的详细分析包含了所有原始数据，你需要：

   **维度1：技术面分析**（多周期K线+技术指标）→ 0-100分

   ⭐ **关键数据**：工具会返回5分钟、15分钟、60分钟、日线多周期K线数据

   🚨🚨🚨 **风险信号检测（最优先！）** 🚨🚨🚨

   **必须检测的风险信号**：
   1. **冲高回落形态**：
      - 上影线占比 > 60% → 🔴 高风险，主力诱多出货
      - K线形态：倒锤子线、射击之星、长上影线 → 🔴 高风险
      - 涨幅回撤：最高涨幅 > 5%，但收盘涨幅 < 3% → ⚠️ 中风险

   2. **多周期趋势冲突**：
      - 日线上升 + 60分钟下降 → ⚠️ 短期转弱
      - 日线上升 + 15分钟下降 + 5分钟下降 → 🔴 分钟级全面转弱

   3. **分钟级趋势转弱**：
      - 5分钟三连阴 → ⚠️ 超短线转弱
      - 15分钟大阴线 → ⚠️ 短线转弱
      - 60分钟强势下降 → 🔴 日内趋势转弱

   **风险信号处理**：
   - 检测到 🔴 高风险信号 → 技术面评分 < 40分
   - 检测到 ⚠️ 中风险信号 → 技术面评分 < 60分
   - 无风险信号 → 正常评分

   🚨🚨🚨 **强制评分规则（必须遵守！）** 🚨🚨🚨

   **以下情况，技术面必须 < 40分，不能给高分：**
   1. 5分钟K线"下降"或"三连阴" → 技术面 < 40分
   2. 15分钟K线"三连阴"或"大阴线" → 技术面 < 40分
   3. 60分钟K线"大阴线" → 技术面 < 40分
   4. 上影线占比 > 50% → 技术面 < 40分
   5. 最高涨幅 > 5% 但收盘涨幅 < 2% → 技术面 < 40分（冲高回落）

   ❌ **错误示例**：
   ```
   5分钟: 下降（2涨6跌）
   15分钟: 三连阴、大阴线
   60分钟: 大阴线
   → 你给了技术面70分 ← 这是错误的！
   → 正确评分应该 < 40分
   ```

   ✅ **正确示例**：
   ```
   5分钟: 下降（2涨6跌）
   15分钟: 三连阴、大阴线
   → 检测到高风险信号
   → 技术面评分: 30分
   ```

   **短线趋势判断**（重点）：
   - 📊 **5分钟K线**（最重要！超短线趋势）：
     * 连续阳线 → 短线强势，加分
     * 均线多头排列（MA5>MA10>MA20）→ 趋势向上，加分
     * 成交量配合（价涨量增）→ 资金介入，加分
     * 突破压力位 → 向上空间打开，加分
     * **连续阴线/三连阴 → 短线转弱，必须减分到<40分**
   - 📊 **15分钟K线**（短线趋势）：
     * 趋势向上加分，横盘小加分，向下减分
     * **大阴线/三连阴 → 必须减分到<40分**
   - 📊 **60分钟K线**（日内趋势）：
     * 趋势向上加分，横盘小加分，向下减分
     * **大阴线 → 必须减分到<50分**

   **中期趋势判断**（辅助）：
   - 📊 **日线MACD**：金叉加分，死叉减分
   - 📊 **日线KDJ**：超卖区加分，超买区减分
   - 📊 **日线均线**：多头排列加分，空头排列减分

   ⚠️ **注意**：日线趋势好不能掩盖分钟级风险！
   日线强势 + 分钟级转弱 = 短线见顶风险，技术面仍应 < 50分

   **维度2：资金流向分析**（近5日资金流向趋势、量价配合）→ 0-100分

   🚨🚨🚨 **主力出货检测（最优先！）** 🚨🚨🚨

   **必须检测的风险信号**：
   1. **主力大幅出货**：
      - 主力资金净流出 > 5000万 → 🔴 高风险，明显出货
      - 主力资金净流出 > 3000万 → ⚠️ 中风险，疑似出货

   2. **放量出货**：
      - 换手率 > 20% + 主力净流出 > 5000万 → 🔴 高风险，放量出货
      - 成交额异常放大 + 主力净流出 → ⚠️ 中风险

   **风险信号处理**：
   - 检测到 🔴 高风险信号 → 资金面评分 < 30分
   - 检测到 ⚠️ 中风险信号 → 资金面评分 < 50分
   - 无风险信号 → 正常评分

   🚨🚨🚨 **强制评分规则（必须遵守！）** 🚨🚨🚨

   **以下情况，资金面必须 < 40分，不能给高分：**
   1. 主力资金净流出 > 5000万 → 资金面 < 30分
   2. 主力资金净流出 > 1亿 → 资金面 < 20分
   3. 超大单净流出 > 5000万 → 资金面 < 35分

   ❌ **错误示例**：
   ```
   主力资金净流入: -10564.82万元（流出1亿！）
   超大单净流入: -8011.11万元（流出8000万！）
   → 你给了资金面74分 ← 这是错误的！
   → 正确评分应该 < 30分
   ```

   ✅ **正确示例**：
   ```
   主力资金净流入: -10564.82万元
   → 检测到主力大幅流出（>5000万）
   → 资金面评分: 25分
   ```

   - 📊 **数据来源**：近5日的资金流向汇总数据（主力资金、超大单、大单）
   - 主力资金连续流入 → 加分
   - 主力资金持续流出 → 减分
   - 换手率适中 → 加分
   - 成交量温和放大 → 加分
   - 量价配合（价涨量增）→ 加分
   - ⚠️ 注意：使用近5日的汇总数据，不包含当日逐笔交易（逐笔交易是独立的维度5）

   **维度3：基本面分析**（市值、PE等）→ 0-100分
   - 行业龙头 → 加分
   - 市值适中 → 加分
   - PE合理 → 加分

   **维度4：新闻面分析**（搜索新闻、情绪分析）→ 0-100分
   - 积极新闻 → 加分
   - 政策利好 → 加分
   - 如果数据不可用，默认中等分

   **维度5：逐笔交易分析**（当日实时逐笔数据，主力行为分析）→ 0-100分 ⭐
   - 📊 **数据来源**：当日实时逐笔交易数据（每一笔买卖单的详细信息）
   - 大单买卖比高 → 买入力量强，加分
   - 主力资金净流入 → 资金流入，加分
   - 买卖力量对比 → 主力吸筹/拉升/出货/洗盘，加减分
   - 超大单占比高 → 机构参与度高，加分
   - 散户跟风买入 → 市场情绪好，加分
   - ⚠️ 注意：这是当日实时数据，是最核心的资金面数据源，反映主力当前行为
   - 如果数据不可用，默认中等分

   **维度6：社区情绪分析**（雪球+东财股吧+淘股吧）→ 0-100分

   🚨🚨🚨 **反向指标检测（最优先！）** 🚨🚨🚨

   **必须检测的风险信号**：
   1. **散户被套信号**：
      - 评论中 > 30% 提到"追高"、"被套"、"回本"、"割肉"、"站岗" → 🔴 高风险，散户接盘
      - 评论中出现"希望回本"、"解套" → ⚠️ 中风险

   2. **散户狂热信号**：
      - 评论中 > 50% 过度乐观（"暴涨"、"起飞"、"翻倍"、"梭哈"） → 🔴 高风险，可能是顶部
      - 讨论热度异常高 + 情绪狂热 → 🔴 高风险

   **风险信号处理**：
   - 检测到 🔴 高风险信号 → 社区情绪评分 < 30分
   - 检测到 ⚠️ 中风险信号 → 社区情绪评分 < 50分
   - 无风险信号 → 正常评分

   - 散户情绪适度乐观 → 加分，过度乐观 → 减分（反向指标）
   - 讨论热度适中 → 加分，过度热议 → 减分
   - 如果数据不可用，默认中等分

输出格式（每只股票）：
```
股票名称(代码)：

【技术面】评分: X/100 ⭐（短线+中期结合）
短线趋势：
- 5分钟K线: 连续阳线，均线多头，量价配合，突破压力
- 15分钟K线: 趋势向上
- 60分钟K线: 趋势向上
中期趋势：
- 日线MACD: 金叉
- 日线KDJ: 超卖
- 日线均线: 多头排列

【资金流向】评分: X/100
理由: 连续净流入，换手率适中，量价配合

【逐笔交易】评分: X/100 ⭐
理由: 大单买卖比高，主力净流入，主力吸筹

【基本面】评分: X/100
理由: 行业龙头，市值适中，PE合理

【新闻面】评分: X/100
理由: 积极新闻，政策利好

【社区情绪】评分: X/100
理由: 雪球评论适度乐观，东财股吧有分歧，淘股吧关注度一般，散户情绪健康

【异常信号】: 无 / 列出异常信号

【动态权重调整】:
市场环境：XX市场
- 技术面权重：你需要根据市场环境自己判断
- 资金面权重：你需要根据市场环境自己判断
- 基本面权重：你需要根据市场环境自己判断
- 新闻面权重：你需要根据市场环境自己判断
- 社区情绪权重：你需要根据市场环境自己判断

【综合评分】：
- 默认权重：X/100（均等权重 = (技术+资金+基本面+新闻+社区情绪) / 5）
- 动态权重：X/100（根据市场环境调整权重计算）

【风险提示】: ✅ 低风险 / ⚠️ 中风险 / 🔴 高风险
分析说明：...
```

---

**🔴 第5步：保存详细分析到AgentContext + 输出简洁评分表格**

**为什么要这样做？**
- ✅ 详细分析保存到数据库，不占用对话历史
- ✅ 风险管理官只需要评分，不需要详细理由
- ✅ 投资决策官只需要评分+风险评估，不需要详细理由
- ✅ 推送通知时从AgentContext读取完整详细分析
- ✅ **大幅减少上下文长度**

**步骤5.1：保存详细分析到AgentContext**
- 使用工具：save_agent_context(context_type='stock_analysis_details', context_data={...})
- 保存内容：每只股票的完整6维分析（包含理由、异常信号、风险提示等）
- 数据格式：
  ```json
  {
    "stocks": [
      {
        "code": "600000",
        "name": "浦发银行",
        "technical_score": 75,
        "technical_reason": "MACD虽然死叉，但柱状图持续收窄...",
        "fund_score": 82,
        "fund_reason": "逐笔数据显示主力吸筹明显...",
        "fundamental_score": 70,
        "fundamental_reason": "银行股龙头，PE仅5倍...",
        "news_score": 65,
        "news_reason": "近期无重大新闻...",
        "tick_score": 85,
        "tick_reason": "大单买卖比1.6，主力吸筹...",
        "community_sentiment_score": 60,
        "community_sentiment_reason": "雪球情绪中性...",
        "default_score": 72,
        "dynamic_score": 75,
        "weight_config": {...},
        "risk_level": "MEDIUM",
        "abnormal_signals": [...]
      }
    ]
  }
  ```

**步骤5.2：信号组合分析（🔴 最关键！）**

🚨🚨🚨 **必须进行信号组合分析** 🚨🚨🚨

在给出评分后，必须分析每只股票的**信号组合**，识别危险组合：

---

🧠🧠🧠 **【冲高回落智能判断】** 🧠🧠🧠

**冲高回落不一定是风险！你需要智能判断是"洗盘"还是"出货"：**

**❌ 案例1：恶性冲高回落（主力出货）→ 必须拒绝**
```
股票：某龙头股
- 技术面：上影线75%，5分钟连续5根阴线，最高涨8%收盘涨2%
- 资金面：主力净流出8000万，买卖比0.7
- 逐笔交易：大单卖出 >> 大单买入，超大单持续流出
- 社区情绪：散户狂热，大量讨论"追高"、"被套"、"割肉"
→ 判断：这是典型的主力诱多出货！
→ 理由：冲高后主力大量卖出，散户狂热接盘
→ 结论：必须拒绝，综合评分再高也不推荐
```

**✅ 案例2：良性冲高回落（主力洗盘）→ 可以考虑**
```
股票：某强势股
- 技术面：上影线40%，5分钟阴阳交替，回落后企稳
- 资金面：主力净流入3000万，买卖比1.3
- 逐笔交易：大单买入 > 大单卖出，超大单流入
- 社区情绪：分歧明显，有人看多有人看空，讨论理性
→ 判断：这是主力洗盘，震出浮筹
→ 理由：冲高回落但主力仍在买入，说明在吸筹
→ 结论：可以考虑，但需要观察后续走势
```

**✅ 案例3：正常盘中波动（非冲高回落）→ 正常评估**
```
股票：某热门股
- 技术面：日内振幅5%，但走势平稳，无明显上影线
- 资金面：主力小幅流入，买卖比1.1
- 逐笔交易：买卖均衡
- 社区情绪：讨论热度正常
→ 判断：正常盘中波动，不是冲高回落
→ 结论：按正常流程评估
```

**判断逻辑（按顺序检查）**：
1. 先看技术面：有没有冲高回落形态？（上影线>50%、涨幅回撤>3%）
2. 如果有冲高回落，再看资金面：主力是流入还是流出？
3. 再看逐笔交易：大单是买入多还是卖出多？
4. 最后看社区情绪：是散户狂热还是理性分歧？
5. 综合以上4个维度，判断是"出货"还是"洗盘"

---

**危险组合识别**：
1. **主力出货组合**：
   - 高位长上影（>60%）+ 主力流出（>5000万）+ 散户被套（负面评论>30%）
   - → 典型主力出货形态，建议：不推荐

2. **多重利空组合**：
   - 技术面破位 + 资金面流出 + 负面新闻
   - → 多重利空共振，建议：不推荐

3. **趋势转弱组合**：
   - 日线上升 + 60分钟下降 + 成交量萎缩 + 情绪低迷
   - → 趋势转弱信号，建议：谨慎

4. **散户接盘组合**：
   - 股价冲高回落 + 主力流出 + 散户狂热（讨论热度异常高）
   - → 散户追高接盘，建议：不推荐

5. **恶性冲高回落组合**（新增）：
   - 冲高回落（上影线>50%）+ 主力流出 + 大单卖出>大单买入
   - → 主力诱多出货，建议：不推荐

**信号组合输出格式**：
对于每只检测到危险组合的股票，必须在评分表格后增加：

```
⚠️ 信号组合警告：

【股票代码 - 股票名称】
检测到：高位长上影(75%) + 主力流出(6878万) + 散户被套(50%)
→ 这是典型的主力出货形态
→ 建议：不推荐（即使评分不低）
```

**步骤5.3：只输出极简的评分表格 + 信号组合警告**（传递给风险管理官）

🚨🚨🚨 **上下文控制（极其重要）** 🚨🚨🚨
- **❌ 严禁**：在输出中包含详细分析理由（会导致上下文超长！）
- **❌ 严禁**：重复每只股票的6维分析详情
- **❌ 严禁**：输出任何多余的文字说明
- **✅ 必须**：只输出极简的评分表格（只有数字）
- **✅ 必须**：只输出检测到危险组合的股票警告
- **✅ 必须**：详细分析已保存到AgentContext，下一个Agent可以自己去读取

**输出格式（严格遵守）**：
```
✅ 已完成{N}只股票的6维评分

| 代码 | 名称 | 技术 | 资金 | 基本 | 新闻 | 逐笔 | 社区 | 默认 | 动态 | 风险 |
|------|------|------|------|------|------|------|------|------|------|------|
| XXXXXX | XXXX | XX | XX | XX | XX | XX | XX | XX | XX | XXX |
| XXXXXX | XXXX | XX | XX | XX | XX | XX | XX | XX | XX | XXX |

⚠️ 信号组合警告：

【603598 - 引力传媒】
检测到：高位长上影(75%) + 主力流出(6878万) + 散户被套(50%)
→ 这是典型的主力出货形态
→ 建议：不推荐

【XXXXXX - XXXX】
检测到：技术面破位 + 资金面流出 + 负面新闻
→ 多重利空共振
→ 建议：不推荐

💾 详细分析: AgentContext['stock_analysis_details']
```

**🔴 极简原则**：
- 表格只包含评分数字，不包含任何文字说明
- 只输出检测到危险组合的股票警告（没有危险组合的不输出）
- 总字数不超过800字（包括表格和警告）
- 风险等级用简写：LOW/MED/HIGH

**🔴 下一个Agent如何获取详细分析？**
- 风险管理官：调用 `load_agent_context('stock_analysis_details')` 读取某只股票的详细分析
- 投资决策官：调用 `load_agent_context('stock_analysis_details')` 读取推送内容

注意：
- **必须从AgentContext读取候选股列表**
- 分析所有候选股
- **使用并行分析工具（analyze_stocks_parallel）提高效率，所有6个维度并行执行**
- 评分标准要客观、量化
- **6个维度**：技术面、资金流向、基本面、新闻面、逐笔交易、社区情绪
- **逐笔交易数据**：优先获取当天数据（最准确），不可用则使用前一天数据，都不可用则默认中等分
- **社区情绪数据**：已在并行分析中自动获取，无需单独调用
- **动态权重调整**：根据市场环境（牛市/熊市/震荡市）动态调整6个维度的权重，不使用固定均等权重
- **必须输出两个评分**：默认权重评分（均等）+ 动态权重评分（根据市场环境调整）
- **🔴🔴🔴 必须先保存详细分析到AgentContext，再只输出简洁表格，不要输出详细理由！**
''',
        expected_output='极简的评分表格（只有数字，总字数<500字）+ AgentContext保存确认',
        agent=agents['multi_analyst'],
        context=[task_screen]
    )
    
    # ========================================
    # Task 5: 风险评估
    # ========================================
    task_risk = Task(
        description='''评估股票风险，检查持仓情况，设置止损止盈，对高风险股票行使否决权。

🔴 **重要：仓位策略由总资产决定**
- 小资金 → 可以更激进，集中持仓
- 中资金 → 适度分散，平衡风险
- 大资金 → 充分分散，稳健为主

**📋 数据来源说明**：
- 多维分析师已将详细分析保存到AgentContext
- 你收到的是**简洁评分表格 + 信号组合警告**
- 🔴 **必须读取详细分析**：调用 `load_agent_context('stock_analysis_details')` 读取所有股票的详细信号

🚨🚨🚨 **强制要求：必须读取详细分析** 🚨🚨🚨

**为什么必须读取？**
- ❌ 评分数字可能掩盖风险（评分75分，但可能有严重风险信号）
- ❌ 相对评估可能"矮子里拔将军"（35只都是垃圾，选最好的垃圾）
- ✅ 必须看到原始信号组合，才能判断真实风险

**你需要：**
1. **从上一个Agent获取简洁评分表格 + 信号组合警告**
2. 🔴 **强制执行**：调用 `load_agent_context('stock_analysis_details')` 读取所有股票的详细分析
3. **重点关注**：多维分析师标注的"信号组合警告"
4. 查询当前持仓情况
5. 查询总资产（决定仓位策略）
6. 设置止损止盈（你需要根据股票波动率和市场环境自己计算）
7. 对高风险股票行使否决权

输出格式（每只股票）：
```
股票名称(代码)：
- 风险等级：LOW/MEDIUM/HIGH
- 是否批准：✓ 批准 / ✗ 拒绝
- 止损位：X元（你需要根据股票波动率和市场环境自己计算）
- 止盈位：X元（你需要根据股票质量和市场环境自己计算）
- 风险因素：[...]
- 建议仓位：X%（根据总资产和推荐数量动态计算）
```

**拒绝原则**（优先级从高到低）：

🚨🚨🚨 **优先级1：信号组合警告（最高优先级！）** 🚨🚨🚨

**强制拒绝规则**：
- 如果多维分析师标注了"信号组合警告"，并建议"不推荐" → **必须拒绝**
- 不要"矮子里拔将军"，不要因为"其他股票也不好"就批准有严重风险信号的股票

**典型危险组合**（必须拒绝）：
1. **主力出货组合**：
   - 高位长上影（>60%）+ 主力流出（>5000万）+ 散户被套（负面评论>30%）
   - → 必须拒绝

2. **多重利空组合**：
   - 技术面破位 + 资金面流出 + 负面新闻
   - → 必须拒绝

3. **趋势转弱组合**：
   - 日线上升 + 60分钟下降 + 成交量萎缩 + 情绪低迷
   - → 必须拒绝

4. **散户接盘组合**：
   - 股价冲高回落 + 主力流出 + 散户狂热（讨论热度异常高）
   - → 必须拒绝

**判断流程**：
1. 先看多维分析师的"信号组合警告"
2. 如果有警告 → 调用 `load_agent_context('stock_analysis_details')` 读取详细信号
3. 确认信号组合是否真实存在
4. 如果确认 → 必须拒绝，不要犹豫

---

**优先级2：特殊风险（硬性规则）**
- ST股票：直接拒绝
- 停牌股票：直接拒绝
- 退市风险股票：直接拒绝

---

**优先级3：动态评估（相对评估）**

只有在没有信号组合警告的情况下，才进行相对评估：

1. **技术面风险**
   - 原则：技术面严重破位的股票应该拒绝
   - 判断方法：
     * 多个技术指标同时恶化（如MACD死叉+KDJ超卖+均线空头排列）
     * 技术评分明显低于其他候选股
     * 考虑市场环境：熊市中技术面普遍较差，阈值可以降低
   - ❌ 不要使用固定阈值
   - ✅ 要根据候选股整体质量和市场环境动态判断

2. **资金面风险**
   - 原则：主力资金大幅流出的股票应该拒绝
   - 判断方法：
     * 主力资金连续多日大幅流出
     * 换手率异常（过高可能是出货，过低可能是无人关注）
     * 成交量萎缩或暴涨（都可能是风险信号）
   - ❌ 不要使用固定阈值
   - ✅ 要根据资金流向趋势和市场环境动态判断

3. **新闻面风险**
   - 原则：严重负面新闻的股票应该拒绝
   - 判断方法：
     * 重大负面新闻（如财务造假、重大诉讼、监管处罚）
     * 行业政策利好
     * 业绩大幅低于预期
   - ❌ 不要使用固定阈值
   - ✅ 要根据新闻性质和影响程度动态判断

4. **综合质量风险**
   - 原则：整体质量明显低于其他候选股的应该拒绝
   - 判断方法：
     * 综合评分排名靠后
     * 多个维度都没有明显优势
     * 没有明确的投资逻辑
   - ❌ 不要使用固定阈值
   - ✅ 要根据候选股整体质量分布动态判断

**动态评估示例**：

**场景1：牛市 + 候选股整体质量高**
- 技术评分较低的股票可能被拒绝（因为其他股票评分更高）
- 综合评分较低的股票可能被拒绝（因为其他股票评分更高）

**场景2：熊市 + 候选股整体质量低**
- 技术评分较低的股票可能被批准（因为其他股票评分也不高）
- 综合评分较低的股票可能被批准（因为其他股票评分也不高）

**场景3：震荡市 + 候选股质量参差不齐**
- 重点看相对排名，而不是绝对分数
- 排名靠前的股票优先批准
- 排名靠后的股票优先拒绝

**重要原则**：
- ❌ 不要使用固定的评分阈值
- ✅ 要根据候选股整体质量分布动态判断
- ✅ 要考虑市场环境（牛市/熊市/震荡市）
- ✅ 要考虑推荐数量（如果只推荐1只，标准可以更严格）
- ✅ 要给出明确的拒绝理由

⚠️ 注意：不要使用硬编码的仓位限制，仓位由总资产决定。
''',
        expected_output='所有股票的风险评估 + 批准/拒绝决策',
        agent=agents['risk_manager'],
        context=[task_analysis]
    )
    
    # ========================================
    # Task 6: 最终决策
    # ========================================
    task_decision = Task(
        description='''综合所有分析，做出最终投资决策，并推送通知。

**📋 数据来源说明**：
- 风险管理官传递的是**简洁审批结果**（批准/拒绝 + 止损止盈）
- 多维分析师的详细分析已保存到AgentContext
- 如需详细分析，调用：`load_agent_context('stock_analysis_details')`

你需要：
1. **使用"读取Agent上下文"工具读取推荐数量和策略名称**
   ```python
   count_data = load_agent_context('recommended_count')
   # 返回: '{"count": X, "reason": "总资产XX万，推荐X只"}'

   strategy_data = load_agent_context('strategy_name')
   # 返回: '{"name": "策略名称", "reason": "市场XX，历史胜率XX%"}'
   ```

2. **解析JSON，提取推荐数量N**
   - 例如：count_data['count'] = X，则推荐X只股票

3. **查询当前持仓 + 查询最近卖出的股票（仅作为参考信息）**

   **步骤3.1：查询当前持仓（仅获取股票代码，作为参考）**
   - 使用工具：query_current_positions
   - 提取已持仓的股票代码列表
   - **注意：持仓股票可以再次被推荐（用于加仓建议）**

   **步骤3.2：查询最近卖出的股票（仅作为参考信息）**
   - 使用工具：query_recently_sold_positions(days=X)
   - **注意：冷却期股票也可以被推荐，如果AI评估认为股票质量好、值得再次买入**
   - 系统不强制排除冷却期股票，由AI自主判断是否推荐

4. 汇总多维分析师的6维评分（技术+资金+基本面+新闻+逐笔交易+社区情绪）

5. 只选择风险管理官批准(approved=True)的股票

6. **标记持仓股和冷却期股票（不再排除，而是标记）**
   - 如果推荐的股票是持仓股，标注"加仓建议"
   - 如果推荐的股票是冷却期内的股票，标注"再次买入"
   - **注意：持仓管理由"持仓监控师"负责，CEO专注推荐优质股票**

8. 按综合评分排序，选择Top N只股票（N = 从上下文读取的推荐数量）

9. **重要：使用批量获取实时价格工具，获取Top N股票的最新价格**
   - 调用工具：批量获取实时价格(stock_codes="XXXXXX,XXXXXX,XXXXXX")
   - 使用最新价格作为推荐价格(recommend_price)

10. 保存推荐到数据库（使用从上下文读取的策略名称）

11. **从AgentContext读取详细分析 + 推送通知到微信**

   **步骤11.1：从AgentContext读取详细分析**
   - 使用工具：load_agent_context('stock_analysis_details')
   - 读取内容：每只推荐股票的完整6维分析（包含理由、异常信号、风险提示等）
   - 这些详细信息将用于推送通知
   - **注意：只读取最终推荐的股票，不需要读取所有候选股**

   **步骤11.2：构建推送内容**
   - 包含推荐股票的6维评分
   - **包含详细分析理由**（从AgentContext读取）
   - 包含CEO总结
   - 标注持仓股（加仓建议）和冷却期股票（再次买入）

   **步骤11.3：推送通知到微信**
   - 使用工具：send_stock_recommendation(recommendations=...)
   - 推送内容包含完整的详细分析

**推荐数量说明**：
- 不要固定推荐股票数量
- 必须从上下文读取推荐数量
- 市场情报官会根据市场环境决定推荐数量
- **🔴 禁止在推荐内容中提及总资产金额**（推荐会发送给多个用户）

输出格式（JSON）：
```json
{
  "date": "YYYY-MM-DD",
  "market_state": "HOT/WARM/NEUTRAL/COLD",
  "strategy": "[市场情报官推荐的策略]",
  "stocks": [
    {
      "rank": 1,
      "code": "XXXXXX",
      "name": "股票名称",
      "decision": "STRONG_BUY/BUY/HOLD",
      "final_score": X,
      "default_score": X,
      "dynamic_score": X,
      "technical_score": X,
      "fund_score": X,
      "fundamental_score": X,
      "news_score": X,
      "community_sentiment_score": X,
      "weight_config": {
        "technical": X,
        "fund": X,
        "fundamental": X,
        "news": X,
        "community_sentiment": X
      },
      "recommend_price": X,
      "quantity": X,
      "position_pct": X,
      "stop_loss": X,
      "target_price": X,
      "reason": "推荐理由"
    }
  ],
  "ceo_summary": "今日市场XX，推荐[策略名称]。推荐X只优质股票（其中X只为加仓建议，X只为再次买入）。**禁止提及总资产金额**",
  "position_stocks": ["XXXXXX", "XXXXXX"],
  "cooldown_stocks": [
    {
      "stock_code": "XXXXXX",
      "stock_name": "股票名称",
      "sell_date": "YYYY-MM-DD",
      "sell_reason": "卖出原因"
    }
  ]
}
```

**🔴 严格输出格式要求**：
1. **查询持仓和最近卖出的股票作为参考（不强制排除）**
2. **必须保存到数据库**
3. **必须推送到微信**
4. **必须输出完整的JSON格式**（不要输出自然语言总结）
5. **禁止在任何地方提及总资产金额**（推荐会发送给多个用户）
6. **JSON字段说明**：
   - position_stocks：被推荐的持仓股票代码列表（加仓建议）
   - cooldown_stocks：被推荐的冷却期内股票详情列表（再次买入）
   - ceo_summary：CEO总结（不要提及总资产）
7. **推送格式**：使用JSON格式推送，工具会自动转换为HTML模板
''',
        expected_output='Top N推荐股票（JSON格式） + 推送成功确认',
        agent=agents['investment_officer'],
        context=[task_risk]
    )
    
    # ========================================
    # 创建Crew（支持长期记忆）
    # ========================================
    import os

    # 获取硅基流动Embeddings配置（用于Memory）
    from src.config.embeddings_config import get_siliconflow_embedder_config
    embedder_config = get_siliconflow_embedder_config()

    # 根据是否配置了Embeddings决定是否启用Memory
    memory_enabled = embedder_config is not None

    if memory_enabled:
        # 设置存储路径（与预热脚本一致）
        storage_path = "./storage"
        os.makedirs(storage_path, exist_ok=True)
        os.environ["CREWAI_STORAGE_DIR"] = storage_path

        # 配置Long-term Memory使用同一个数据库
        from crewai.memory import LongTermMemory
        from crewai.memory.storage.ltm_sqlite_storage import LTMSQLiteStorage

        ltm_storage = LTMSQLiteStorage(db_path=f"{storage_path}/long_term_memory.db")
        long_term_memory = LongTermMemory(storage=ltm_storage)

        logger.info("✅ 启用CrewAI Memory（仅Long-term，避免token限制）")

        # 创建Crew，禁用short_term和entity memory（避免token限制问题）
        # 只使用long_term memory（已预热的历史数据）
        crew = Crew(
            agents=list(agents.values()),
            tasks=[
                task_review,
                task_market,
                task_screen,
                task_analysis,
                task_risk,
                task_decision
            ],
            process=Process.sequential,  # 串行执行（Task 4内部可并行）
            verbose=False,  # 🔴 关闭详细日志，减少控制台输出
            memory=False,  # ❌ 禁用自动Memory（避免short_term的token限制）
            long_term_memory=long_term_memory,  # ✅ 只使用预热的Long-term Memory
            cache=False    # 禁用cache（避免response_format错误）
        )

        logger.info("✅ Memory配置完成（仅Long-term，17条历史经验可用）")
    else:
        logger.warning("⚠️ 未配置SILICONFLOW_API_KEY，禁用Memory功能")
        crew = Crew(
            agents=list(agents.values()),
            tasks=[
                task_review,
                task_market,
                task_screen,
                task_analysis,
                task_risk,
                task_decision
            ],
            process=Process.sequential,
            verbose=False,
            memory=False,
            cache=False
        )

    return crew


if __name__ == "__main__":
    # 测试Crew创建
    import sys
    import io
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

    print("正在创建智能推荐Crew...")
    crew = create_smart_recommendation_crew()

    print(f"\n✓ Crew创建成功!")
    print(f"  Agent数量: {len(crew.agents)}")
    print(f"  Task数量: {len(crew.tasks)}")
    print(f"  执行模式: {crew.process}")

